package org.java_tutorials.class12;

public class LinkList {
    Node startNode;
    int nodeIndex;
    public void add(int i)
    {
        Node n=new Node(i,null);
        if (nodeIndex==0)
        {
            //startNode=
        }
    }
}
